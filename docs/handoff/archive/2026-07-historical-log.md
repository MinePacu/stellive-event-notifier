# Historical handoff log

This file is archived historical context only.

Agents must not read this file during normal feature work, merge-only work, routine debugging, or routine PR review.

Use only when the user explicitly requests historical context, when investigating a regression that requires old implementation history, or when a current handoff document links to this exact file.

No unambiguously July-dated entries were present in the previous `docs/AI_HANDOFF.md` at archival time. July strings in that document described event date ranges, so the preserved legacy handoff remains in `2026-06-historical-log.md` rather than being split incorrectly.
